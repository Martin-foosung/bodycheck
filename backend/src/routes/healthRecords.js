import {Router} from 'express';import {PrismaClient} from '@prisma/client';import {auth} from '../middleware/auth.js';const db=new PrismaClient();const r=Router();r.get('/',auth,async(req,res)=>res.json(await db.healthRecord.findMany({where:{userId:req.user.sub},orderBy:{createdAt:'desc'},take:100})));r.post('/',auth,async(req,res)=>{const b=req.body||{};const row=await db.healthRecord.create({data:{userId:req.user.sub,bodyPath:b.path||[],bodyPart:(b.path||[]).at(-1)||b.bodyPart||'',symptom:b.symptom||'',intensity:b.symptomMeta?.intensity,onset:b.symptomMeta?.onset,duration:b.symptomMeta?.duration,trend:b.symptomMeta?.trend,answers:b.answers||{},riskLevel:b.riskLevel||null,department:b.department||null}});res.status(201).json(row)});export default r;
